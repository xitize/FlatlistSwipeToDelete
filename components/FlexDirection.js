import React, { Component } from 'react'
import {
    View, Text,
} from 'react-native'
import { TextInput } from 'react-native-gesture-handler';

export default class FlexDirection extends Component {

    render() {
        return (
            <View style={{ flex: 1, flexDirection: 'column', marginTop: 20, alignItems: 'center', justifyContent: 'center' }}>
               
            </View>
        )
    }
}